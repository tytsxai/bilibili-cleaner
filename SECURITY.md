# 安全说明 / Security

## 威胁模型

本项目是**本地自托管工具**，不是多租户服务。设计前提：

- 运行服务的人 == 被清理账号的所有者。
- 服务只监听本机，或部署在使用者信任的网络里。

在这个前提之外使用（比如直接把端口开到公网），下面的限制会变成真实风险。

## 服务本身没有认证

`SESSDATA` / `bili_jct` 是**逐请求传入的 B 站凭据**，不是本服务的登录凭据。服务不校验调用方是谁。

后果：**任何能访问该端口的人都能通过它向 B 站发请求**（包括未认证的
`GET /api/qrcode` 扫码登录流程）。因此：

- `docker-compose.yml` 默认绑定 `127.0.0.1:8000`，不对外暴露。
- 需要远程访问时，必须在前面放反向代理，由代理提供 TLS 与认证。见 [docs/DEPLOY.md](docs/DEPLOY.md)。

`/api/v2/tasks/*` 的任务按创建者归属：每个任务记录 `SESSDATA` 的 SHA-256 摘要，
查询、列表和取消都只对同一凭据可见。**注册表内不保存可用的凭据本身**，摘要也不会出现在
日志或 API 响应里。用别的凭据访问他人任务返回 404 而不是 403——返回 403 等于确认该
任务 id 存在。

这仍不能替代网络层防护：任何能访问端口的人都可以用自己的凭据正常使用本服务。

## 凭据的存放位置

| 位置 | 内容 | 风险 |
|------|------|------|
| 浏览器 `sessionStorage` | Web UI 登录后的 `SESSDATA` / `bili_jct` | 仅限当前标签页，关闭即清除。旧版本用的 `localStorage` 会在页面加载时自动清理 |
| `~/.bilibili-cleaner/credentials.json` | CLI 凭据 | 保存时 `chmod 600`；可用 `BILI_CREDENTIALS_PATH` 改位置 |
| 环境变量 `BILI_SESSDATA` / `BILI_JCT` | CLI 凭据 | 优先级高于文件；注意别写进 shell history |

服务端**不落盘任何凭据**，也不把凭据写进日志。

## 日志与审计中的敏感信息

- 应用日志记录 method / path / 状态码 / 耗时 / request id，不记录请求头和 cookie。
- 审计日志（`data/audit.jsonl`）记录被删除对象的 id（mid、动态 id、收藏项 id）。
  这属于账号行为数据，请按对待个人数据的方式保管。
- B 站返回的错误体会原样回传给调用方（`data` 字段），便于排障；这些内容来自
  bilibili.com，不含本地凭据。

## 不可逆操作

所有删除都不可恢复，B 站没有回收站。缓解措施：

- 建议流程是"先列出 → 本地筛选 → 人工复核 → 再删除"，而不是直接一键清空。
- 可以先把候选 UP 加进 B 站关注分组（`/api/v2/relation-tags/*`）复核。
- 审计日志记录每一次删除，用于事后核对。见 [docs/DEPLOY.md](docs/DEPLOY.md#6-审计日志与恢复)。

## 依赖

- `constraints.txt` 固定了部署所用的版本，`docker build` 可复现。
- `backend/requirements.txt` 带上界，避免主版本升级在安装期直接打挂。
- 升级依赖时请跑 `pytest tests/` 后再一起提交。

## 报告问题

如发现安全问题，请通过仓库 issue 描述**复现方式和影响**，不要在公开 issue 里贴真实
`SESSDATA` 或其他凭据。
