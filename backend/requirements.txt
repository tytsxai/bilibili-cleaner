# Upper bounds guard against a major release breaking the app at install time.
# For deployments also apply the exact pins: -c constraints.txt
fastapi>=0.104.0,<1.0
uvicorn>=0.24.0,<1.0
httpx>=0.25.0,<1.0
qrcode>=7.4.0,<9.0
pillow>=10.0.0,<14.0
python-multipart>=0.0.6,<1.0
typer>=0.12.0,<1.0
pydantic>=2.0,<3.0
